// I'm using this for now as while I am adding it to config_default, 
// the people already playing have a config.cfg so they won't get the 
// settings until they get a fresh config.cfg (I ASSUME)

mp_decals "1024"
r_decals "2048"
zoom_sensitivity_ratio 0.5
fps_max 300
sv_master_legacy_mode 0 // This fixes the issue of servers not being listed correctly in the browser. Thanks valve.